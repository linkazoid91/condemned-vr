# Reproducible line endings.
* text=auto eol=lf

# Windows scripts use CRLF in the working tree.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files are never line-ending normalized if deliberately whitelisted.
*.png binary
*.exe binary
*.dll binary
*.pdb binary
